version: '2'
services:

  bitcoind-api-fe:
    image: bitcoind-api-fe:latest
    build:
      context: .
      dockerfile: $PWD/Dockerfile
    ports:
      - "4001:3000"
    networks:
      - bitcoind
    volumes:
      - .:/usr/src/app

networks:
  bitcoind:
    external: true
